# Link support files
ln -s $HOME/Repos/shDotFiles/.swift-format
ln -s $HOME/Repos/shDotFiles/.swiftlint.yml

# Add to support files to .gitignore
echo .swift-format >> .gitignore
echo .swiftlint.yml >> .gitignore

# Initialize repo
git init &> /dev/null
